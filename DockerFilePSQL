FROM postgres:latest

# Optional: Set environment variables for password and database name (recommended for security)
ENV POSTGRES_USER=byte_cat
ENV POSTGRES_PASSWORD=byte_cat
ENV POSTGRES_DB=image_db

# Copy initialization script (if using)
COPY init.sql /docker-entrypoint-initdb.d/

EXPOSE 5432
